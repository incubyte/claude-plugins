{
  "name": "discovery",
  "version": "0.3.1",
  "description": "End-to-end product discovery flow that produces a structured PRD. Runs ten phases (context → scope → competition → journeys → wireframes → mockups → epics → tech → metrics → GTM → assembly) with checkpoints between phases. Resumable across sessions.",
  "author": {
    "name": "Incubyte"
  },
  "license": "Apache-2.0"
}
